
Route::group(
    [
        'prefix' => config('dotzone.cms_route_prefix'),
        'middleware' => ['auth']
    ], function(){

    Route::view('dashboard', 'manage.dashboard')->name('dashboard');

    Route::get('users', [\App\Http\Controllers\UserController::class, 'index'])->name('users.index');

    // Addition routes
});
